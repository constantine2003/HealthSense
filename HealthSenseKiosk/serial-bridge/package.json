{
  "name": "healthsense-serial-bridge",
  "version": "1.0.0",
  "description": "Serial-to-WebSocket bridge for HealthSense ESP32 kiosk",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.9.0",
    "serialport": "^12.0.0",
    "ws": "^8.18.0"
  }
}
